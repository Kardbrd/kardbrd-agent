# Kardbrd Agent Environment Configuration
#
# Copy to ~/.config/kardbrd-agent/agent.env and edit.
# Board subscriptions are stored in state files, configured via:
#   kardbrd-agent sub <setup-url>

# Container runtime: "podman" or "docker" (auto-detected if empty)
#CONTAINER_CMD=podman

# Anthropic API key (optional if ~/.claude is mounted with credentials)
#ANTHROPIC_API_KEY=sk-ant-...

# Logging: DEBUG, INFO (default), WARNING, ERROR
LOG_LEVEL=INFO

# Git identity (alternative to mounting .gitconfig)
#GIT_AUTHOR_NAME=Kardbrd Agent
#GIT_AUTHOR_EMAIL=agent@example.com
#GIT_COMMITTER_NAME=Kardbrd Agent
#GIT_COMMITTER_EMAIL=agent@example.com
