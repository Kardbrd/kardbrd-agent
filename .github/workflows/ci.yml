name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Pre-commit hooks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          check-latest: true
          cache: true

      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Install pre-commit
        run: python -m pip install --upgrade pip pre-commit

      - name: Run pre-commit
        run: pre-commit run --all-files

  go:
    name: Go checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          check-latest: true
          cache: true

      - name: Download modules
        run: go mod download

      - name: Check formatting
        run: |
          set -euo pipefail
          files="$(gofmt -l $(git ls-files '*.go'))"
          if [ -n "$files" ]; then
            echo "$files"
            exit 1
          fi

      - name: Vet
        run: go vet ./...

      - name: Test
        run: go test -race ./...

      - name: Build binary
        run: go build -trimpath -o /tmp/kardbrd ./cmd/kardbrd

  docs:
    name: Docs build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Install MkDocs
        run: python -m pip install --upgrade pip 'mkdocs-material>=9.5,<10'

      - name: Build docs
        run: mkdocs build --strict

  package-smoke:
    name: Release package smoke test (${{ matrix.goos }}/${{ matrix.goarch }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - goos: linux
            goarch: amd64
          - goos: linux
            goarch: arm64
          - goos: darwin
            goarch: amd64
          - goos: darwin
            goarch: arm64
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          check-latest: true
          cache: true

      - name: Build release-shaped archive
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: |
          set -euo pipefail
          version="ci-${GITHUB_SHA::12}"
          archive_dir="kardbrd_${version}_${GOOS}_${GOARCH}"
          mkdir -p "dist/${archive_dir}"
          CGO_ENABLED=0 GOOS="${GOOS}" GOARCH="${GOARCH}" go build \
            -trimpath \
            -ldflags="-s -w -X github.com/Kardbrd/kardbrd-agent/internal/version.Version=${version}" \
            -o "dist/${archive_dir}/kardbrd" \
            ./cmd/kardbrd
          tar -C dist -czf "dist/${archive_dir}.tar.gz" "${archive_dir}"
          cd dist
          sha256sum "${archive_dir}.tar.gz" > checksums.txt
          sha256sum -c checksums.txt
          tar -tzf "${archive_dir}.tar.gz" | sort > archive-contents.txt
          printf '%s/\n%s/kardbrd\n' "${archive_dir}" "${archive_dir}" | sort | diff -u - archive-contents.txt
          case "${GOOS}/${GOARCH}" in
            linux/amd64) format='ELF 64-bit.*x86-64' ;;
            linux/arm64) format='ELF 64-bit.*aarch64' ;;
            darwin/amd64) format='Mach-O 64-bit.*x86_64' ;;
            darwin/arm64) format='Mach-O 64-bit.*arm64' ;;
          esac
          file "${archive_dir}/kardbrd" | grep -Eq "${format}"

      - uses: actions/upload-artifact@v7
        with:
          name: kardbrd-ci-${{ matrix.goos }}-${{ matrix.goarch }}
          path: dist/*.tar.gz
          if-no-files-found: error
          retention-days: 7
